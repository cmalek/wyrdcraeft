Lexicon Architecture (superseded)
=================================

.. note::
   The separate lexicon read model and ``search_keys`` search index were removed
   in Phase B of the unified dictionary workflow. Browse search and detail loading
   now live in the dictionary package and query ``bt_*`` tables directly.

See :doc:`dictionary` for the current dictionary build, query, and browse
architecture.
